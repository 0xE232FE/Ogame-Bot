# Ogame-Bot

# Usage
```
docker run -itd --name awesome-ogame-bot -e UNIVERSE='YOUR_UNIVERSE' -e USERNAME='YOUR_USERNAME' -e PASSWORD='YOUR_PASSWORD' -e LANGUAGE='YOUR_LANG' -v '/etc/ssl/certs/ca-certificates.crt:/etc/ssl/certs/ca-certificates.crt' herklos/ogame-bot
```
